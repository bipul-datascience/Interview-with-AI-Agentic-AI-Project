services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: interview_ready
      POSTGRES_USER: interview_user
      POSTGRES_PASSWORD: change_me
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
